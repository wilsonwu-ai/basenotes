# This package intentionally supports local mode only.
BASENOTE_APP_MODE=local
HOST=127.0.0.1
PORT=3000

# Do not put Shopify, Appstle, database, payment, or Cloudflare credentials here.
# Local startup deliberately refuses if common Shopify secret variables are set.
