<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ shop.name }} - Coming Soon</title>

  {{ 'https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap' | stylesheet_tag }}

  <style>
    :root {
      --color-primary: #1a1a1a;
      --color-secondary: #c9a86c;
      --color-text: #1a1a1a;
      --color-text-light: #666;
      --color-border: #e5e5e5;
      --color-error: #f44336;
      --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-body);
      background: linear-gradient(135deg, var(--color-primary) 0%, #2a2a2a 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .password-page {
      max-width: 480px;
      width: 100%;
      text-align: center;
    }

    .password-page__logo {
      margin-bottom: 3rem;
    }

    .password-page__logo svg {
      width: 80px;
      height: auto;
      color: white;
    }

    .password-page__brand {
      font-size: 1.5rem;
      font-weight: 700;
      color: white;
      margin-top: 1rem;
      letter-spacing: 0.1em;
    }

    .password-page__card {
      background: white;
      border-radius: 16px;
      padding: 3rem 2rem;
      margin-bottom: 2rem;
    }

    .password-page__badge {
      display: inline-block;
      padding: 0.25rem 1rem;
      background: var(--color-secondary);
      color: var(--color-primary);
      font-size: 0.7rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      border-radius: 20px;
      margin-bottom: 1.5rem;
    }

    .password-page__title {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 0.75rem;
      color: var(--color-primary);
    }

    .password-page__message {
      color: var(--color-text-light);
      font-size: 1rem;
      line-height: 1.6;
      margin-bottom: 2rem;
    }

    .password-page__form {
      margin-bottom: 1.5rem;
    }

    .password-page__input-group {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }

    .password-page__input {
      flex: 1;
      padding: 0.875rem 1rem;
      font-size: 1rem;
      border: 2px solid var(--color-border);
      border-radius: 8px;
      outline: none;
      transition: border-color 0.2s;
    }

    .password-page__input:focus {
      border-color: var(--color-secondary);
    }

    .password-page__btn {
      padding: 0.875rem 1.5rem;
      background: var(--color-primary);
      color: white;
      font-size: 0.875rem;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.2s;
    }

    .password-page__btn:hover {
      background: #2a2a2a;
    }

    .password-page__error {
      color: var(--color-error);
      font-size: 0.875rem;
      margin-top: 0.5rem;
    }

    .password-page__divider {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin: 1.5rem 0;
      color: var(--color-text-light);
      font-size: 0.875rem;
    }

    .password-page__divider::before,
    .password-page__divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--color-border);
    }

    .password-page__newsletter p {
      font-size: 0.875rem;
      color: var(--color-text-light);
      margin-bottom: 1rem;
    }

    .password-page__newsletter-form {
      display: flex;
      gap: 0.5rem;
    }

    .password-page__footer {
      color: rgba(255, 255, 255, 0.5);
      font-size: 0.875rem;
    }

    .password-page__footer a {
      color: var(--color-secondary);
      text-decoration: none;
    }

    .password-page__admin {
      margin-top: 1rem;
    }

    .password-page__admin a {
      color: rgba(255, 255, 255, 0.5);
      font-size: 0.75rem;
      text-decoration: underline;
    }

    @media (max-width: 480px) {
      .password-page__card {
        padding: 2rem 1.5rem;
      }

      .password-page__input-group,
      .password-page__newsletter-form {
        flex-direction: column;
      }

      .password-page__btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="password-page">
    <div class="password-page__logo">
      <svg viewBox="0 0 80 120" fill="none" stroke="currentColor" stroke-width="1.5">
        <rect x="20" y="30" width="40" height="80" rx="4"/>
        <rect x="30" y="10" width="20" height="20"/>
        <rect x="35" y="0" width="10" height="10" rx="2"/>
        <text x="40" y="78" text-anchor="middle" font-size="12" fill="currentColor" stroke="none">BN</text>
      </svg>
      <div class="password-page__brand">{{ shop.name | upcase }}</div>
    </div>

    <div class="password-page__card">
      <span class="password-page__badge">Coming Soon</span>
      <h1 class="password-page__title">Something Special is Brewing</h1>
      <p class="password-page__message">
        We're putting the finishing touches on our new collection of luxury fragrances. Enter the password to get early access, or subscribe to be notified when we launch.
      </p>

      {%- form 'storefront_password', class: 'password-page__form' -%}
        <div class="password-page__input-group">
          <input
            type="password"
            name="password"
            class="password-page__input"
            placeholder="Enter store password"
            autocomplete="current-password"
          >
          <button type="submit" class="password-page__btn">Enter</button>
        </div>
        {%- if form.errors -%}
          <p class="password-page__error">{{ form.errors.messages.password }}</p>
        {%- endif -%}
      {%- endform -%}

      <div class="password-page__divider">or</div>

      <div class="password-page__newsletter">
        <p>Be the first to know when we launch</p>
        {%- form 'customer', class: 'password-page__newsletter-form' -%}
          <input type="hidden" name="contact[tags]" value="password-page,prospect">
          <input
            type="email"
            name="contact[email]"
            class="password-page__input"
            placeholder="Enter your email"
            required
          >
          <button type="submit" class="password-page__btn">Notify Me</button>
        {%- endform -%}
      </div>
    </div>

    <footer class="password-page__footer">
      <p>&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. All rights reserved.</p>
      <div class="password-page__admin">
        <a href="/admin">Store owner? Log in here</a>
      </div>
    </footer>
  </div>
</body>
</html>
